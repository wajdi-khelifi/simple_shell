#include "simple_shell.h"

/**
 * setenv - Set or update an environment variable.
 * @name: The name of the environment variable to set/update.
 * @overwrite: If non-zero, overwrite the variable if it exists;
 * otherwise, keep the original value.
 *
 * Return: If successful, returns 0; otherwise, returns -1 on error.
 */
int setenv(const char *name, const char *value, int overwrite)
{
	return (0);
}

/**
 * unsetenv - Unset or remove an environment variable.
 * @name: The name of the environment variable to unset.
 *
 * Return: If successful, returns 0; otherwise, returns -1 on error.
 */
int unsetenv(const char *name)
{
	return (0);
}
